title: David HC
description: ""
permalink: /:path/
url: "https://daavidhc.com"

# Defaults
defaults:
  - scope:
      path: "_posts"
    values:
      layout: post
      permalink: /:title

sass:
  style: compressed

# HTML Compression
compress_html:
  clippings: all
  endings: all
